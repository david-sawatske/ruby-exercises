# Using the Ruby language, have the function letter_changes(str) take the str
# parameter being passed and modify it using the following algorithm. Replace
# every letter in the string with the letter following it in the alphabet
# (ie. c becomes d, z becomes a). Then capitalize every vowel in this
# new string (a, e, i, o, u) and finally return this modified string.

def letter_changes(str)

end

p letter_changes('hello*3') #== 'Ifmmp*3'
p letter_changes('fun times!') == 'gvO Ujnft!'
